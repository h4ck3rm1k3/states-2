interface: 0.0.0.0

worker_threads: 2

file_roots:
  base:
    - /etc/salt/states
    - /etc/salt/states/formulas/apt
    - /etc/salt/states/formulas/mysql
    - /etc/salt/pillars
    - /etc/salt/pillars/files

pillar_roots:
  base:
    - /etc/salt/pillars

state_verbose: False